const email=document.querySelector('input.my-form-control#email');
const password=document.querySelector('input.my-form-control#password');

email.addEventListener('focus',(e)=>{
    // console.log(e.target.placeholder);
    // e.target.placeholder;
})
